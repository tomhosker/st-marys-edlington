extends layout

block content
    h1 #{title}

    if otherDocuments.length == 0
        p #[i None as yet.]
    else
        table
            tr
                th ID
                th Name
                th Date
                th Link
            for document in otherDocuments
                tr
                    td #{document.id}
                    td #{document.name}
                    td #{document.date}
                    td [#[a(href=document.link) here]]
